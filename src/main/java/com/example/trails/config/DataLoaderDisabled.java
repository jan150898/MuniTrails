package com.example.trails.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DataLoaderDisabled {
    // Data loader disabled for now - sample data can be added via API
}
